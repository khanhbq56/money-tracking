from django.urls import path
from . import views

app_name = 'ai_chat'

urlpatterns = [
    # API endpoints will be added in Phase 2/5
    # path('chat/process/', views.process_chat_message, name='process_chat'),
    # path('chat/confirm/', views.confirm_transaction, name='confirm_transaction'),
] 